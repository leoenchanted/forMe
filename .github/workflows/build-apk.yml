name: Build Android APK & Release
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: 🏗 Build & Release
    runs-on: ubuntu-latest
    env:
      APP_VARIANT: preview

    steps:
      - name: 📦 Check out Git repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # 🔥 核心：增加 Swap 空间，防止 OOM 爆内存 (保留你的优化)
      - name: 💾 Increase Swap Space
        run: |
          # 清理系统垃圾
          sudo apt clean
          sudo apt autoremove -y
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/share/boost
          
          # 精简 Android SDK（只保留必要组件）
          sudo rm -rf /usr/local/lib/android/sdk/{system-images,emulator,sources,docs}
          
          # 只保留一个 platform（如 android-34）
          if [ -d "/usr/local/lib/android/sdk/platforms/android-34" ]; then
            sudo mv /usr/local/lib/android/sdk/platforms/android-34 /tmp/
            sudo rm -rf /usr/local/lib/android/sdk/platforms/*
            sudo mv /tmp/android-34 /usr/local/lib/android/sdk/platforms/
          fi

      # 💾 创建 3GB Swap（足够防 OOM，又不至于吃光磁盘）
      - name: 💾 Create 3GB Swap (Balance RAM vs Disk)
        run: |
          echo "Current free space:"
          df -h
          sudo fallocate -l 3G /swapfile
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile
          echo "Swap enabled:"
          sudo swapon --show

      # 读取 app.config.js 里的版本号 (记得本地要把 version 改成 1.1.5)
      - name: 📖 Get Version
        id: get_version
        # 这里用 grep 提取 version，因为 app.config.js 是 JS 文件不是 JSON
        # 这种写法比较粗暴但有效：提取 version: "x.x.x" 中的 x.x.x
        run: |
          VER=$(grep -o 'version: "[^"]*"' app.config.js | cut -d'"' -f2)
          echo "APP_VERSION=$VER" >> $GITHUB_OUTPUT
          echo "Detected Version: $VER"

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18.x
          cache: npm

      - name: ⚙️ Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Install dependencies
        run: npm install

      # 使用 --clear-cache 确保干净构建
      - name: 🚀 Build APK (Local)
        run: eas build --platform android --profile preview --non-interactive --local --clear-cache --output=./forme.apk

      # 🧹 构建后立即释放所有缓存（包括 swap 文件！）
      - name: 🧹 Post-build cleanup
        if: always()
        run: |
          # 删除 swap 文件，回收磁盘
          sudo swapoff /swapfile || true
          sudo rm -f /swapfile || true
          
          # 清理构建缓存
          rm -rf ~/.gradle/caches ~/.gradle/wrapper
          rm -rf $HOME/.npm/_cacache
          
          echo "Final disk usage:"
          df -h

      - name: 🎉 Create Release
        uses: ncipollo/release-action@v1
        with:
          tag: "v${{ steps.get_version.outputs.APP_VERSION }}"
          name: "v${{ steps.get_version.outputs.APP_VERSION }} - Super App Upgrade"
          body: |
            ## 🚀 v1.1.5 里程碑更新

            ### 🌟 核心功能升级
            - **DeepGlow Studio 集成**: 新增 WebGL 图像处理工具，支持辉光、色温调节，并可保存至相册。
            - **全新架构**: 采用 Bottom Tab + Stack 混合导航，操作更流畅。
            - **自定义首页**: 支持更换首页壁纸，集成天气与每日语录组件。

            ### 🎨 UI/UX 交互焕新
            - **悬浮 Tab 栏**: 全新设计的椭圆悬浮底部导航，带有平滑的背景跟随动画。
            - **列表优化**: VibeWall 列表采用动态高度瀑布流，并增加横/竖屏角标提示。
            - **沉浸式体验**: 优化全屏浏览和下载页面的视觉反馈。

            ### 📥 下载中心 2.0
            - **智能管理**: 支持任务排队、去重、失败重试。
            - **数据可视化**: 显示具体的下载进度 (MB) 和状态。
            - **权限修复**: 完美适配 Android 13+ 媒体权限。

            ---
            *Built with ❤️ by GitHub Actions*
          artifacts: "./forme.apk"
          token: ${{ secrets.GITHUB_TOKEN }}
          allowUpdates: true
