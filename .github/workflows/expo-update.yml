name: Auto Publish Update (Manual)
on:
  workflow_dispatch: # 手动触发（仅网页点击）
    inputs:
      message:
        description: 'Update Message'
        required: true
        default: 'General bug fixes'
jobs:
  update:
    name: 🚀 EAS Update
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Check out Git repository
        uses: actions/checkout@v4
        with:
          ref: main # 新增：固定使用 main 分支（热更新专用）
      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18.x
          cache: npm
      - name: ⚙️ Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}
      - name: 📦 Install dependencies
        run: npm install
      # 🔥 核心：发布热更新（仅基于 main 分支）
      - name: 🚀 Publish Update
        run: |
          eas update --branch preview --message "${{ github.event.inputs.message }}"