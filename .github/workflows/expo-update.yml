name: Auto Publish Update (Manual)
on:
  workflow_dispatch: #只有我去网页点按钮才发，防止手滑
    inputs:
      message:
        description: 'Update Message'
        required: true
        default: 'General bug fixes'

jobs:
  update:
    name: 🚀 EAS Update
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Check out Git repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18.x
          cache: npm

      - name: ⚙️ Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }} # 还是用之前配置好的那个 Token

      - name: 📦 Install dependencies
        run: npm install

      # 🔥 核心步骤：发布热更新
      - name: 🚀 Publish Update
        run: |
          # 使用你在网页输入的描述
          eas update --branch preview --message "${{ github.event.inputs.message }}"